<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/consumer_milky"
    android:orientation="vertical">

    <!-- Main Content Area -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingTop="24dp">

        <!-- Header -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:paddingBottom="16dp"
            android:paddingStart="24dp"
            android:paddingEnd="24dp"
            android:text="My Order"
            android:textColor="@color/foodshare_text"
            android:textSize="24sp"
            android:textStyle="bold" />

        <!-- Interactive order status tabs -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            app:cardBackgroundColor="@color/consumer_lime_light"
            app:cardCornerRadius="0dp"
            app:cardElevation="0dp"
            app:strokeColor="@color/consumer_lime"
            app:strokeWidth="1dp">

            <com.google.android.material.tabs.TabLayout
                android:id="@+id/tabsOrders"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@color/consumer_lime_light"
            app:tabIndicatorColor="@color/consumer_lime"
            app:tabIndicatorHeight="5dp"
            app:tabMode="fixed"
            app:tabGravity="fill"
            app:tabRippleColor="@color/consumer_lime_light"
            app:tabSelectedTextColor="@color/foodshare_dark_green"
                app:tabTextColor="@color/foodshare_text_secondary" />

        </com.google.android.material.card.MaterialCardView>

        <!-- Order List & Empty State Container -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rvOrderHistory"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />

            <TextView
                android:id="@+id/tvEmptyState"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:text="No orders found"
                android:textSize="15sp"
                android:textColor="@color/foodshare_text_secondary"
                android:visibility="gone" />

        </RelativeLayout>

    </LinearLayout>

    <!-- Bottom Navigation Bar -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/consumer_milky"
        app:itemIconTint="@color/bottom_nav_color"
        app:itemTextColor="@color/bottom_nav_color"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/bottom_nav_menu" />

</LinearLayout>
